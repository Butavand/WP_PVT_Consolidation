// Build TIMELINE

var timeline = [];

timeline.push(whichParticipant, whichVersion);

timeline.push(
    instructionsPractice,
    practiceRunEncodingA, practiceRunEncodingB,
    practiceRunTestA, practiceRunTestB,
);

        timeline.push(instructionsFirstTrial);
        timeline.push(
            runEncodingA, runEncodingB,
            instructionsBreak
        )
        
        timeline.push(
            pvtStartScreen1,
            pvtStartScreen2, 
            pvtPracticeLoop, 
            pvtMainInstructions, 
            pvtMainProcedure, 
            ifMissingFeedback,
            pvtEnd
        );
     
    timeline.push(
        instructionsTest,
        runTestA, runTestB
    );  
